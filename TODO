Extra filtering for fsstat

In print, just print out the given statistic if specified (and use the
same approach for the kstat cli emulator in jkstat); this partially works

Scale graphs

Custom title for graphs

Leverage ChartableKstat
